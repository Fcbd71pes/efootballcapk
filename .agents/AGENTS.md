# Customization Rules

- When creating or updating an implementation plan (`implementation_plan.md`), write the entire document, goals, proposed changes, and explanations in Bengali (বাংলা) instead of English.
